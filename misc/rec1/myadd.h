#ifndef __MYADD_H__ // if not defined, define it and do everything until #endif
#define __MYADD_H__ // all the header files need this GUARD/hack/magic
// w/o it can end up with inf loops

int add(int a, int b);


#endif
